[project]
name = "devhub-storage"
version = "0.1.0"
description = "Storage interface and adapters for DevHub (Google Drive, SharePoint, local)"
requires-python = ">=3.11"
dependencies = [
    "devhub-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
devhub-core = { workspace = true }
